<!DOCTYPE html>
<html>
  <head>
    <title>Mon projet GitHub</title>
  </head>
  <body>
    <h1>Vfor Talent</h1>
    <div id="iframes-container"></div>
    <script>
      const iframesContainer = document.getElementById("iframes-container");
      
      // Récupérer le contenu du fichier de référence sur GitHub à l'aide de l'API
      fetch('https://api.github.com/repos/Vfortalent/Liste-Index/contents/liste-de-reference.txt')
        .then(response => response.json())
        .then(data => {
          // Convertir les données du fichier en chaîne de caractères
          const referenceList = atob(data.content);
          
          // Parcourir la liste de référence et créer dynamiquement une iframe pour chaque site web
          referenceList.split('\n').forEach(siteUrl => {
            const trimmedSiteUrl = siteUrl.trim();
            if (trimmedSiteUrl.length > 0) {
              const iframe = document.createElement("iframe");
              iframe.src = trimmedSiteUrl;
              iframesContainer.appendChild(iframe);
            }
          });
        })
        .catch(error => console.error(error));
    </script>
  </body>
</html>
<html>
<head>
	<title>Basculer entre une vue en grille et une vue en liste</title>
	<style>
		/* Styles pour les images en grille */
		.grid-container {
			display: grid;
			grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
			grid-gap: 20px;
		}

		.grid-item {
			border: 1px solid black;
			padding: 20px;
			text-align: center;
		}

		/* Styles pour les images en liste */
		.list-container {
			display: flex;
			flex-direction: column;
			align-items: center;
			gap: 20px;
		}

		.list-item {
			border: 1px solid black;
			padding: 20px;
			text-align: center;
			width: 100%;
			box-sizing: border-box;
		}
	</style>
</head>
<html>
